# KIẾN TRÚC HỆ THỐNG - MICROSERVICES

## 1. TỔNG QUAN KIẾN TRÚC

### 1.1. Mô hình tổng thể
Hệ thống sử dụng kiến trúc **Microservices** với các đặc điểm:
- **Loosely coupled services**: Mỗi service độc lập, communicate qua API
- **Technology diversity**: Sử dụng FastAPI (Python) cho backend services
- **Containerization**: Mỗi service chạy trong Docker container riêng
- **Database per service**: Mỗi service có database riêng (hoặc schema riêng)
- **API Gateway**: Centralized entry point cho tất cả requests
- **Service Discovery**: Tự động phát hiện và routing

### 1.2. Lý do chọn Microservices
**Ưu điểm:**
- **Scalability**: Scale từng service độc lập theo nhu cầu
- **Technology flexibility**: Dễ dàng thay đổi công nghệ cho từng service
- **Fault isolation**: Lỗi ở một service không ảnh hưởng toàn hệ thống
- **Team autonomy**: Team khác nhau phát triển service khác nhau
- **Continuous deployment**: Deploy từng service mà không ảnh hưởng toàn bộ

**Nhược điểm (và cách giải quyết):**
- **Complexity**: Sử dụng Docker Compose để đơn giản hóa deployment
- **Data consistency**: Sử dụng event-driven architecture khi cần
- **Monitoring**: Tập trung logging và monitoring

### 1.3. Sơ đồ kiến trúc tổng quan

```
┌──────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐         │
│  │   Browser     │  │    Mobile     │  │    Tablet     │         │
│  │  (React/Vue)  │  │    Browser    │  │    Browser    │         │
│  └───────────────┘  └───────────────┘  └───────────────┘         │
└────────────────────────────┬─────────────────────────────────────┘
                             │ HTTPS
                             ↓
┌──────────────────────────────────────────────────────────────────┐
│                    API GATEWAY (Nginx)                           │
│              Load Balancing + Rate Limiting + CORS               │
│                    Route: /api/v1/{service}/...                  │
└────────────────────────────┬─────────────────────────────────────┘
                             │
            ┌────────────────┼────────────────┐
            │                │                │
            ↓                ↓                ↓
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────-┐
│  Auth Service    │ │  Asset Service   │ │Procurement Service│
│  (FastAPI)       │ │  (FastAPI)       │ │  (FastAPI)        │
│  Port: 8001      │ │  Port: 8002      │ │  Port: 8003       │
└────────┬─────────┘ └────────┬─────────┘ └────────┬─────────-┘
         │                    │                    │
         ↓                    ↓                    ↓
┌─────────────────-─┐ ┌──────────────────┐ ┌───────────────--───┐
│Maintenance Service│ │ Report Service   │ │Notification Service│
│  (FastAPI)        │ │  (FastAPI)       │ │  (FastAPI)         │
│  Port: 8004       │ │  Port: 8005      │ │  Port: 8006        │
└────────┬─────────-┘ └────────┬─────────┘ └────────┬─────────--┘
         │                     │                    │
         └─────────────────-───┼────────────────────┘
                               ↓
┌──────────────────────────────────────────────────────────────────┐
│                        SHARED INFRASTRUCTURE                     │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐      │
│  │  PostgreSQL    │  │     Redis      │  │  File Storage  │      │
│  │  (Databases)   │  │  (Cache/Queue) │  │    (Volume)    │      │
│  └────────────────┘  └────────────────┘  └────────────────┘      │
│  ┌────────────────┐  ┌────────────────┐                          │
│  │  RabbitMQ/Kafka│  │   Elasticsearch│  (Optional)              │
│  │ (Message Queue)│  │   (Logging)    │                          │
│  └────────────────┘  └────────────────┘                          │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │
│  │ SMTP Server  │  │  AWS S3      │  │  Monitoring  │            │
│  │   (Email)    │  │(File backup) │  │(Prometheus)  │            │
│  └──────────────┘  └──────────────┘  └──────────────┘            │
└──────────────────────────────────────────────────────────────────┘
```

## 2. MICROSERVICES BREAKDOWN

### 2.1. Auth Service (Authentication & Authorization)
**Trách nhiệm:**
- Đăng nhập (Local + Active Directory)
- Multi-Factor Authentication (MFA) với OTP
- JWT token generation & validation
- Refresh token management
- Password reset
- User management (CRUD)
- Role & Permission management
- Active Directory synchronization

**Technology Stack:**
- **Framework**: FastAPI
- **ORM**: SQLAlchemy
- **Auth**: python-jose (JWT), passlib (password hashing)
- **MFA/OTP**: pyotp (TOTP generator)
- **Active Directory**: ldap3 (LDAP client)
- **Database**: PostgreSQL (schema: auth_db)

**API Endpoints:**
- `POST /api/v1/auth/login` (Step 1: Email/Password)
- `POST /api/v1/auth/verify-otp` (Step 2: OTP verification)
- `POST /api/v1/auth/logout`
- `POST /api/v1/auth/refresh`
- `POST /api/v1/auth/forgot-password`
- `GET /api/v1/auth/mfa/setup` (Generate QR code for MFA setup)
- `POST /api/v1/auth/mfa/enable` (Enable MFA)
- `POST /api/v1/auth/mfa/disable` (Disable MFA)
- `GET/POST/PUT /api/v1/auth/users`
- `GET/POST /api/v1/auth/roles`
- `POST /api/v1/auth/sync-ad` (Sync users from Active Directory)

**Database Tables:**
- users
  - user_type: `local` | `active_directory`
  - mfa_enabled: boolean
  - mfa_secret: encrypted TOTP secret
  - ad_sync_id: Active Directory user ID
  - require_passwd_change: Require to change password next login
- roles
- permissions
- user_roles
- refresh_tokens
- password_reset_tokens
- mfa_backup_codes (for recovery)

**Docker Container:**
```yaml
auth-service:
  build: ./services/auth
  ports:
    - "8001:8000"
  environment:
    - DATABASE_URL=postgresql://user:pass@postgres/auth_db
    - JWT_SECRET=secret
    - REDIS_URL=redis://redis:6379
    - MFA_ISSUER=AssetManagement
    - AD_SERVER=ldap://ad.company.com
    - AD_DOMAIN=company.com
    - AD_BIND_DN=CN=admin,DC=company,DC=com
    - AD_BIND_PASSWORD=ad_password
    - AD_SEARCH_BASE=OU=Users,DC=company,DC=com
  depends_on:
    - postgres
    - redis
```

### 2.2. Asset Service (Quản lý Tài sản)
**Trách nhiệm:**
- CRUD assets (thiết bị, tài sản)
- Asset categories, groups
- Asset assignment & reclaim
- Depreciation calculation
- QR code generation
- File attachment management
- Asset search, filter, pagination
- Import/Export assets

**Technology Stack:**
- **Framework**: FastAPI
- **ORM**: SQLAlchemy
- **QR**: python-qrcode
- **Excel**: openpyxl, pandas
- **Database**: PostgreSQL (schema: asset_db)

**API Endpoints:**
- `GET/POST/PUT/DELETE /api/v1/assets`
- `GET /api/v1/assets/{id}`
- `GET /api/v1/assets/search`
- `POST /api/v1/assets/{id}/assign`
- `POST /api/v1/assets/{id}/reclaim`
- `GET /api/v1/assets/{id}/qrcode`
- `POST /api/v1/assets/import`
- `GET /api/v1/assets/export`
- `GET/POST /api/v1/asset-categories`

**Database Tables:**
- assets
- asset_categories
- asset_groups
- asset_assignments
- asset_files
- asset_history

**Docker Container:**
```yaml
asset-service:
  build: ./services/asset
  ports:
    - "8002:8000"
  environment:
    - DATABASE_URL=postgresql://user:pass@postgres/asset_db
    - FILE_STORAGE_PATH=/app/uploads
  volumes:
    - asset-uploads:/app/uploads
  depends_on:
    - postgres
```

### 2.3. Procurement Service (Quản lý Mua sắm)
**Trách nhiệm:**
- Purchase request workflow
- Multi-level approval process
- Vendor quotation management
- Contract management
- Purchase order management
- Receiving & acceptance
- Department management

**Technology Stack:**
- **Framework**: FastAPI
- **ORM**: SQLAlchemy
- **Workflow**: Custom state machine
- **Database**: PostgreSQL (schema: procurement_db)

**API Endpoints:**
- `GET/POST/PUT /api/v1/purchase-requests`
- `POST /api/v1/purchase-requests/{id}/submit`
- `POST /api/v1/purchase-requests/{id}/approve`
- `POST /api/v1/purchase-requests/{id}/reject`
- `GET/POST /api/v1/quotations`
- `POST /api/v1/quotations/{id}/evaluate`
- `GET/POST /api/v1/contracts`
- `GET/POST/PUT /api/v1/purchase-orders`
- `POST /api/v1/purchase-orders/{id}/confirm`
- `POST /api/v1/purchase-orders/{id}/receive`
- `GET/POST /api/v1/vendors`
- `GET/POST /api/v1/departments`

**Database Tables:**
- purchase_requests
- approval_history
- quotation_requests
- quotations
- contracts
- purchase_orders
- order_items
- vendors
- departments

**Docker Container:**
```yaml
procurement-service:
  build: ./services/procurement
  ports:
    - "8003:8000"
  environment:
    - DATABASE_URL=postgresql://user:pass@postgres/procurement_db
  depends_on:
    - postgres
    - rabbitmq
```

### 2.4. Maintenance Service (Quản lý Bảo trì)
**Trách nhiệm:**
- Maintenance schedule management
- Repair request workflow
- Maintenance history tracking
- Cost tracking
- Maintenance alerts

**Technology Stack:**
- **Framework**: FastAPI
- **ORM**: SQLAlchemy
- **Scheduling**: APScheduler
- **Database**: PostgreSQL (schema: maintenance_db)

**API Endpoints:**
- `GET/POST/PUT /api/v1/maintenance-schedules`
- `GET/POST /api/v1/repair-requests`
- `POST /api/v1/repair-requests/{id}/assign`
- `POST /api/v1/repair-requests/{id}/resolve`
- `GET/POST /api/v1/maintenance-records`
- `GET /api/v1/assets/{id}/maintenance-history`

**Database Tables:**
- maintenance_schedules
- repair_requests
- maintenance_records
- maintenance_costs

**Docker Container:**
```yaml
maintenance-service:
  build: ./services/maintenance
  ports:
    - "8004:8000"
  environment:
    - DATABASE_URL=postgresql://user:pass@postgres/maintenance_db
  depends_on:
    - postgres
```

### 2.5. Report Service (Báo cáo & Thống kê)
**Trách nhiệm:**
- Dashboard statistics
- Custom reports generation
- Export to Excel/PDF
- Data aggregation from multiple services
- Analytics

**Technology Stack:**
- **Framework**: FastAPI
- **ORM**: SQLAlchemy
- **Charts**: matplotlib (backend), Chart.js (frontend)
- **Export**: openpyxl (Excel), ReportLab (PDF)
- **Database**: Read-replica của các services khác

**API Endpoints:**
- `GET /api/v1/reports/dashboard`
- `GET /api/v1/reports/assets-by-department`
- `GET /api/v1/reports/depreciation`
- `GET /api/v1/reports/maintenance-costs`
- `GET /api/v1/reports/procurement-summary`
- `POST /api/v1/reports/export`

**Database:**
- Aggregate data từ các services khác qua API calls
- Hoặc read từ read-replica database

**Docker Container:**
```yaml
report-service:
  build: ./services/report
  ports:
    - "8005:8000"
  environment:
    - ASSET_SERVICE_URL=http://asset-service:8000
    - PROCUREMENT_SERVICE_URL=http://procurement-service:8000
    - MAINTENANCE_SERVICE_URL=http://maintenance-service:8000
```

### 2.6. Notification Service (Thông báo)
**Trách nhiệm:**
- In-app notifications
- Email notifications
- SMS notifications (optional)
- Event subscription & publishing
- Notification templates
- User notification preferences

**Technology Stack:**
- **Framework**: FastAPI
- **Email**: aiosmtplib, Jinja2 templates
- **Message Queue**: RabbitMQ/Redis
- **Database**: PostgreSQL (schema: notification_db)

**API Endpoints:**
- `GET /api/v1/notifications`
- `PUT /api/v1/notifications/{id}/read`
- `GET/PUT /api/v1/notification-preferences`
- `POST /api/v1/notifications/send` (internal)

**Event Consumers:**
- `purchase_request.submitted` → Email to approver
- `purchase_request.approved` → Email to requester
- `asset.assigned` → Email to user
- `maintenance.due` → Email to admin
- `warranty.expiring` → Email to admin

**Database Tables:**
- notifications
- notification_templates
- notification_preferences
- notification_history

**Docker Container:**
```yaml
notification-service:
  build: ./services/notification
  ports:
    - "8006:8000"
  environment:
    - DATABASE_URL=postgresql://user:pass@postgres/notification_db
    - SMTP_HOST=smtp.gmail.com
    - SMTP_PORT=587
    - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
  depends_on:
    - postgres
    - rabbitmq
```

## 3. SHARED INFRASTRUCTURE

### 3.1. API Gateway
**Technology**: Nginx hoặc Kong
**Trách nhiệm:**
- Reverse proxy & load balancing
- Rate limiting
- CORS handling
- SSL termination
- Request routing based on URL path
- Authentication middleware (verify JWT)

**Nginx Configuration Example:**
```nginx
upstream auth-service {
    server auth-service:8000;
}

upstream asset-service {
    server asset-service:8000;
}

server {
    listen 80;
    server_name api.assetmanagement.com;

    location /api/v1/auth/ {
        proxy_pass http://auth-service/api/v1/auth/;
    }

    location /api/v1/assets/ {
        proxy_pass http://asset-service/api/v1/assets/;
    }

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_req zone=api_limit burst=20;
}
```

### 3.2. PostgreSQL Database
**Version**: 14+
**Architecture**: Single instance với multiple schemas

**Schemas:**
- `auth_db`: Auth Service
- `asset_db`: Asset Service
- `procurement_db`: Procurement Service
- `maintenance_db`: Maintenance Service
- `notification_db`: Notification Service

**Alternative**: Separate database instances cho mỗi service (true microservices)

**Docker Container:**
```yaml
postgres:
  image: postgres:14-alpine
  environment:
    POSTGRES_USER: admin
    POSTGRES_PASSWORD: secret
    POSTGRES_MULTIPLE_DATABASES: auth_db,asset_db,procurement_db,maintenance_db,notification_db
  volumes:
    - postgres-data:/var/lib/postgresql/data
    - ./scripts/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
  ports:
    - "5432:5432"
```

### 3.3. Redis
**Use cases:**
- Session storage
- Cache (API responses, frequently accessed data)
- Rate limiting counters
- Job queue (celery)

**Docker Container:**
```yaml
redis:
  image: redis:7-alpine
  ports:
    - "6379:6379"
  volumes:
    - redis-data:/data
```

### 3.4. RabbitMQ (Message Queue)
**Use cases:**
- Event-driven communication giữa services
- Asynchronous task processing
- Notification triggers

**Events:**
- `purchase_request.submitted`
- `purchase_request.approved`
- `purchase_request.rejected`
- `asset.created`
- `asset.assigned`
- `maintenance.scheduled`
- `warranty.expiring`

**Docker Container:**
```yaml
rabbitmq:
  image: rabbitmq:3-management-alpine
  ports:
    - "5672:5672"     # AMQP port
    - "15672:15672"   # Management UI
  environment:
    RABBITMQ_DEFAULT_USER: guest
    RABBITMQ_DEFAULT_PASS: guest
```

### 3.5. File Storage
**Options:**
1. **Docker Volume**: Shared volume giữa các services
2. **Object Storage**: MinIO (S3-compatible)

**Docker Volume:**
```yaml
volumes:
  asset-uploads:
    driver: local
```

**MinIO (Optional):**
```yaml
minio:
  image: minio/minio
  ports:
    - "9000:9000"
    - "9001:9001"
  environment:
    MINIO_ROOT_USER: admin
    MINIO_ROOT_PASSWORD: password
  command: server /data --console-address ":9001"
  volumes:
    - minio-data:/data
```

## 4. INTER-SERVICE COMMUNICATION

### 4.1. Synchronous Communication (HTTP/REST)
**Khi nào sử dụng:**
- Request-response immediate
- Cần dữ liệu ngay lập tức

**Example:**
- Report Service gọi Asset Service để lấy danh sách assets
- Procurement Service gọi Auth Service để verify user permissions

**Implementation:**
- `httpx` (async HTTP client trong FastAPI)
- Service discovery: Environment variables chứa URLs

### 4.2. Asynchronous Communication (Events)
**Khi nào sử dụng:**
- Fire-and-forget
- Multiple consumers
- Decoupling services

**Example:**
- Purchase Request được approved → Publish event `purchase_request.approved`
  - Notification Service nhận event → Gửi email
  - Procurement Service nhận event → Cập nhật workflow

**Implementation:**
- RabbitMQ với Pika library
- Event structure:
```json
{
  "event_type": "purchase_request.approved",
  "timestamp": "2025-10-17T10:00:00Z",
  "data": {
    "request_id": "PR-2025-001",
    "requester_id": 123,
    "approver_id": 456
  }
}
```

## 5. FASTAPI SERVICE STRUCTURE

### 5.1. Project Structure (per service)
```
service-name/
├── app/
│   ├── __init__.py
│   ├── main.py                 # FastAPI app entry point
│   ├── config.py               # Configuration, environment vars
│   ├── dependencies.py         # Dependency injection
│   ├── models/                 # SQLAlchemy models
│   │   ├── __init__.py
│   │   ├── user.py
│   │   └── ...
│   ├── schemas/                # Pydantic schemas (request/response)
│   │   ├── __init__.py
│   │   ├── user.py
│   │   └── ...
│   ├── api/                    # API routes
│   │   ├── __init__.py
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── endpoints/
│   │   │   │   ├── users.py
│   │   │   │   └── ...
│   │   │   └── router.py
│   ├── core/                   # Core functionality
│   │   ├── __init__.py
│   │   ├── security.py         # JWT, password hashing
│   │   ├── database.py         # DB connection
│   │   └── events.py           # Event publishers/subscribers
│   ├── services/               # Business logic
│   │   ├── __init__.py
│   │   ├── user_service.py
│   │   └── ...
│   └── utils/                  # Utilities
│       ├── __init__.py
│       └── helpers.py
├── tests/
│   ├── __init__.py
│   ├── test_users.py
│   └── ...
├── Dockerfile
├── requirements.txt
└── .env.example
```

### 5.2. FastAPI main.py Example
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.core.database import engine
from app.models import Base
from app.api.v1.router import api_router
from app.core.events import startup_event, shutdown_event

# Create tables
Base.metadata.create_all(bind=engine)

app = FastAPI(
    title="Asset Management - Auth Service",
    version="1.0.0",
    docs_url="/api/docs",
    redoc_url="/api/redoc"
)

# CORS
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Include routers
app.include_router(api_router, prefix="/api/v1")

# Startup & shutdown events
@app.on_event("startup")
async def startup():
    await startup_event()

@app.on_event("shutdown")
async def shutdown():
    await shutdown_event()

@app.get("/health")
async def health_check():
    return {"status": "healthy"}
```

### 5.3. SQLAlchemy Model Example
```python
from sqlalchemy import Column, Integer, String, Boolean, DateTime
from sqlalchemy.sql import func
from app.core.database import Base

class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    email = Column(String, unique=True, index=True, nullable=False)
    full_name = Column(String, nullable=False)
    hashed_password = Column(String, nullable=False)
    is_active = Column(Boolean, default=True)
    role_id = Column(Integer, ForeignKey("roles.id"))
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), onupdate=func.now())
```

### 5.4. Pydantic Schema Example
```python
from pydantic import BaseModel, EmailStr
from datetime import datetime
from typing import Optional

class UserBase(BaseModel):
    email: EmailStr
    full_name: str

class UserCreate(UserBase):
    password: str

class UserUpdate(BaseModel):
    full_name: Optional[str] = None
    is_active: Optional[bool] = None

class UserResponse(UserBase):
    id: int
    is_active: bool
    created_at: datetime

    class Config:
        from_attributes = True
```

### 5.5. API Endpoint Example
```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from app.core.database import get_db
from app.schemas.user import UserCreate, UserResponse
from app.services import user_service

router = APIRouter(prefix="/users", tags=["users"])

@router.post("/", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
async def create_user(
    user_data: UserCreate,
    db: Session = Depends(get_db)
):
    existing_user = user_service.get_by_email(db, user_data.email)
    if existing_user:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="Email already registered"
        )
    return user_service.create(db, user_data)

@router.get("/{user_id}", response_model=UserResponse)
async def get_user(
    user_id: int,
    db: Session = Depends(get_db)
):
    user = user_service.get_by_id(db, user_id)
    if not user:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="User not found"
        )
    return user
```

## 6. DOCKER DEPLOYMENT

### 6.1. Complete Docker Compose File
```yaml
version: '3.8'

services:
  # Infrastructure
  postgres:
    image: postgres:14-alpine
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: asset_management
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - backend

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - backend

  rabbitmq:
    image: rabbitmq:3-management-alpine
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - backend

  # API Gateway
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - auth-service
      - asset-service
      - procurement-service
    networks:
      - backend

  # Microservices
  auth-service:
    build:
      context: ./services/auth
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://admin:secret@postgres/asset_management
      - JWT_SECRET=${JWT_SECRET}
      - REDIS_URL=redis://redis:6379
    depends_on:
      - postgres
      - redis
    networks:
      - backend
    restart: unless-stopped

  asset-service:
    build:
      context: ./services/asset
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://admin:secret@postgres/asset_management
      - FILE_STORAGE_PATH=/app/uploads
      - AUTH_SERVICE_URL=http://auth-service:8000
    volumes:
      - asset-uploads:/app/uploads
    depends_on:
      - postgres
    networks:
      - backend
    restart: unless-stopped

  procurement-service:
    build:
      context: ./services/procurement
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://admin:secret@postgres/asset_management
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
      - AUTH_SERVICE_URL=http://auth-service:8000
    depends_on:
      - postgres
      - rabbitmq
    networks:
      - backend
    restart: unless-stopped

  maintenance-service:
    build:
      context: ./services/maintenance
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://admin:secret@postgres/asset_management
      - ASSET_SERVICE_URL=http://asset-service:8000
    depends_on:
      - postgres
    networks:
      - backend
    restart: unless-stopped

  report-service:
    build:
      context: ./services/report
      dockerfile: Dockerfile
    environment:
      - ASSET_SERVICE_URL=http://asset-service:8000
      - PROCUREMENT_SERVICE_URL=http://procurement-service:8000
      - MAINTENANCE_SERVICE_URL=http://maintenance-service:8000
    networks:
      - backend
    restart: unless-stopped

  notification-service:
    build:
      context: ./services/notification
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgresql://admin:secret@postgres/asset_management
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
    depends_on:
      - postgres
      - rabbitmq
    networks:
      - backend
    restart: unless-stopped

  # Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - nginx
    networks:
      - backend

networks:
  backend:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  asset-uploads:
```

### 6.2. Service Dockerfile Example
```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application code
COPY ./app ./app

# Expose port
EXPOSE 8000

# Run FastAPI with Uvicorn
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
```

### 6.3. Requirements.txt (FastAPI Service)
```
fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
pydantic==2.5.0
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
httpx==0.25.1
aiofiles==23.2.1
pika==1.3.2
redis==5.0.1
openpyxl==3.1.2
pandas==2.1.3
python-qrcode[pil]==7.4.2
jinja2==3.1.2
aiosmtplib==3.0.1
pyotp==2.9.0           # For MFA/TOTP
ldap3==2.9.1           # For Active Directory integration
```

## 7. BẢO MẬT

### 7.1. Authentication & Authorization

#### 7.1.1. Multi-Factor Authentication (MFA)
**Two-Step Login Process:**
1. **Step 1**: Email/Password authentication
   - Validate credentials (local hoặc Active Directory)
   - Return temp_token (valid 5 minutes)
2. **Step 2**: OTP verification
   - User nhập OTP từ Authenticator app
   - Validate OTP với TOTP secret
   - Return access_token + refresh_token

**MFA Setup Flow:**
1. User enable MFA trong settings
2. System generate TOTP secret
3. Return QR code (format: `otpauth://totp/AssetManagement:user@email.com?secret=SECRET&issuer=AssetManagement`)
4. User quét QR bằng Google Authenticator / Authy
5. User nhập OTP để verify
6. System lưu mfa_secret (encrypted) vào database

**Backup Codes:**
- Generate 10 one-time backup codes khi enable MFA
- User có thể dùng backup code thay OTP
- Mỗi code chỉ dùng 1 lần

**Active Directory Integration:**
```python
import ldap3

def authenticate_ad(username, password):
    server = ldap3.Server(AD_SERVER, get_info=ldap3.ALL)
    conn = ldap3.Connection(
        server,
        user=f"{username}@{AD_DOMAIN}",
        password=password,
        authentication=ldap3.SIMPLE
    )

    if conn.bind():
        # Search for user info
        search_filter = f"(sAMAccountName={username})"
        conn.search(
            search_base=AD_SEARCH_BASE,
            search_filter=search_filter,
            attributes=['mail', 'displayName', 'department']
        )
        user_info = conn.entries[0]
        return {
            "email": user_info.mail.value,
            "full_name": user_info.displayName.value,
            "department": user_info.department.value
        }
    return None
```

**JWT-based Authentication:**
- Access Token: 8 giờ
- Refresh Token: 7 ngày
- Lưu Refresh Token trong database, có thể revoke

**Password Security:**
- Bcrypt hashing (cost factor: 12)
- Password policy: Min 8 chars, chữ hoa + chữ thường + số
- AD users: password được verify qua LDAP

**API Security:**
- Tất cả endpoints (trừ login) yêu cầu JWT token
- Middleware verify token ở API Gateway và mỗi service
- Rate limiting: 100 req/min/IP

### 7.2. Inter-Service Authentication
**Option 1**: Service-to-Service JWT
- Mỗi service có JWT riêng khi gọi service khác
- Short-lived token (1 giờ)

**Option 2**: Mutual TLS (mTLS)
- Services authenticate nhau qua certificates

### 7.3. Data Security
- **Encryption at rest**: PostgreSQL encryption
- **Encryption in transit**: HTTPS, TLS for RabbitMQ
- **Sensitive data**: Encrypt trong database (email, phone)
- **Secrets management**: Environment variables, Docker secrets

### 7.4. Input Validation
- Pydantic schemas validate tất cả inputs
- SQLAlchemy ORM prevent SQL injection
- File upload validation: type, size
- XSS prevention: Escape outputs

## 8. MONITORING & LOGGING

### 8.1. Logging
**Centralized Logging với ELK Stack (Optional):**
- **Elasticsearch**: Store logs
- **Logstash**: Process logs
- **Kibana**: Visualize logs

**Log Format (JSON):**
```python
import logging
import json

class JSONFormatter(logging.Formatter):
    def format(self, record):
        log_data = {
            "timestamp": self.formatTime(record),
            "level": record.levelname,
            "service": "auth-service",
            "message": record.getMessage(),
            "extra": record.__dict__.get("extra", {})
        }
        return json.dumps(log_data)
```

**Log Levels:**
- ERROR: Exceptions, errors
- WARNING: Deprecated, warnings
- INFO: Request logs, important events
- DEBUG: Detailed debugging (only dev)

### 8.2. Health Checks
Mỗi service expose `/health` endpoint:
```python
@app.get("/health")
async def health_check(db: Session = Depends(get_db)):
    try:
        # Check DB connection
        db.execute("SELECT 1")
        return {"status": "healthy", "database": "ok"}
    except Exception as e:
        return {"status": "unhealthy", "database": "error", "error": str(e)}
```

Docker Compose health check:
```yaml
auth-service:
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
    interval: 30s
    timeout: 10s
    retries: 3
    start_period: 40s
```

### 8.3. Metrics (Optional - Prometheus)
```yaml
prometheus:
  image: prom/prometheus
  volumes:
    - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
  ports:
    - "9090:9090"

grafana:
  image: grafana/grafana
  ports:
    - "3001:3000"
  depends_on:
    - prometheus
```

## 9. SCALABILITY & PERFORMANCE

### 9.1. Horizontal Scaling
```yaml
asset-service:
  build: ./services/asset
  deploy:
    replicas: 3  # Run 3 instances
  environment:
    - DATABASE_URL=postgresql://admin:secret@postgres/asset_management
```

Load balancer (Nginx) sẽ distribute traffic.

### 9.2. Caching Strategy
**Redis Cache:**
- Cache frequently accessed data (asset details, categories)
- TTL: 5-60 phút
- Cache invalidation khi data update

```python
import redis
import json

redis_client = redis.Redis(host='redis', port=6379, decode_responses=True)

async def get_asset(asset_id: int, db: Session):
    # Try cache first
    cache_key = f"asset:{asset_id}"
    cached = redis_client.get(cache_key)
    if cached:
        return json.loads(cached)

    # Query DB
    asset = db.query(Asset).filter(Asset.id == asset_id).first()
    if asset:
        # Cache it
        redis_client.setex(cache_key, 300, json.dumps(asset.dict()))
    return asset
```

### 9.3. Database Optimization
- **Connection Pooling**: SQLAlchemy pool_size=20
- **Indexing**: Index trên foreign keys, search fields
- **Query Optimization**: Select only needed columns, use joins
- **Read Replicas**: Read từ replica, write to master

### 9.4. Asynchronous Processing
- Heavy tasks (export Excel, send emails) → Celery + RabbitMQ
- Background jobs không block API response

## 10. CI/CD PIPELINE

### 10.1. GitHub Actions Workflow
```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          cd services/auth
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run tests
        run: |
          cd services/auth
          pytest --cov=app tests/

      - name: Lint
        run: |
          pip install flake8
          flake8 services/auth/app --max-line-length=120

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build Docker images
        run: |
          docker-compose build

      - name: Push to Docker Hub
        run: |
          echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
          docker-compose push

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /app/asset-management
            git pull origin main
            docker-compose pull
            docker-compose up -d
```

## 11. DISASTER RECOVERY

### 11.1. Backup Strategy
**Database Backup:**
```bash
# Daily backup script
docker exec postgres pg_dump -U admin asset_management | gzip > backup_$(date +%Y%m%d).sql.gz

# Upload to S3
aws s3 cp backup_$(date +%Y%m%d).sql.gz s3://backups/postgres/
```

**File Storage Backup:**
- Sync uploads volume to S3 daily
- Retention: 30 days

### 11.2. Recovery Procedures
**Database Recovery:**
```bash
# Restore from backup
gunzip -c backup_20251017.sql.gz | docker exec -i postgres psql -U admin asset_management
```

**RTO**: < 4 giờ
**RPO**: < 24 giờ

## 12. DEVELOPMENT WORKFLOW

### 12.1. Local Development
```bash
# Start all services
docker-compose up -d

# View logs
docker-compose logs -f auth-service

# Run tests
docker-compose exec auth-service pytest

# Access database
docker-compose exec postgres psql -U admin asset_management
```

### 12.2. Hot Reload
Uvicorn với `--reload` flag → Auto reload khi code changes

### 12.3. API Documentation
FastAPI auto-generate docs:
- Swagger UI: `http://localhost:8001/api/docs`
- ReDoc: `http://localhost:8001/api/redoc`

---

**Phiên bản**: 2.0
**Ngày tạo**: 17/10/2025
**Cập nhật**: 17/10/2025
**Người tạo**: Solution Architect
**Trạng thái**: Updated - Microservices Architecture
**Tài liệu liên quan**:
- [20251017_Project_Overview.md](20251017_Project_Overview.md)
- [20251017_Business_Requirements.md](20251017_Business_Requirements.md)
- [20251017_Database_Design.md](20251017_Database_Design.md)
